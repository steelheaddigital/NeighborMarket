<%= form_for @user, :url => { :action => "create", :controller => "user" }, :html => { :id => 'NewUserForm', :class => 'well' } do |f| %>
	<div>
		<% @user.errors.full_messages.each do |msg| %>
	      <p><%= msg %></p>
	    <% end %>
	</div>
    <div>
      <%= f.label "Email" %>
      <%= f.text_field :email, :class => "input-large" %>
    </div>
	<div>
	  <%= f.submit("Create User", :class => 'btn') %>
	</div>
<% end %>