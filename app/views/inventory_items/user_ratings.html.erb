<div class="container-fluid">
	<%@inventory_items.each do |inventory_item|%>
		<div class="well-fluid row">
		  <div class="col-md-3 col-sm-4">
		    <%if inventory_item.photo?%>
		      <%= image_tag inventory_item.photo.url(:medium), class: "img-responsive" %>
		    <%else%>
		      No Image Available
		    <%end%>
		  </div>
		  <div class="col-md-9 col-sm-8 itemDescription">
		    <h2>
		      <%= item_name(inventory_item) %>
		    </h2>
		    <p><%= inventory_item.description %></p>
		    <p>
				<label>Sold By:</label>
				<%if inventory_item.user.photo?%>
					<a href="<%=user_path(inventory_item.user.id)%>" class="inventorySellerLink"><%= image_tag inventory_item.user.photo.url(:thumb), class: "img-responsive" %><%=inventory_item.user.username%></a>
				<%else%>
					<%= link_to(inventory_item.user.username, user_path(inventory_item.user.id)) %>
				<%end%>
			</p>
	        <%= form_tag rate_inventory_item_path(inventory_item), {class: "form"} do %>
				<div class="form-group">
					<% @rating = inventory_item.ratings.where(user_id: current_user.id).first %>
					<%= radio_button_tag "rating", 1, !@rating.nil? && @rating.rating == 1, class: "star" %>
					<%= radio_button_tag "rating", 2, !@rating.nil? && @rating.rating == 2, class: "star" %>
					<%= radio_button_tag "rating", 3, !@rating.nil? && @rating.rating == 3, class: "star" %>
					<%= radio_button_tag "rating", 4, !@rating.nil? && @rating.rating == 4, class: "star" %>
					<%= radio_button_tag "rating", 5, !@rating.nil? && @rating.rating == 5, class: "star" %>
					<div class="clearfix"></div>
				</div>
	        <% end %>
		  </div>
		</div>
	<%end%>
</div>